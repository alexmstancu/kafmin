package org.kafmin.util;

import java.util.concurrent.Callable;

// TODO probably I don't need this, but I'll laeve it for the moment
public class TryCatchSurrounder {
    public static <T> T surround(Callable<T> callable) {
        return null;
    }
}
